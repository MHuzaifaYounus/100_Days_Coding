"use strict";
// Question 59: Add a Special Number: Make a program that creates custom adders. These adders can add a specific number to any other number you give them later.
// Explain & TIP: Imagine you have a magic box that can add a specific number to any number you put in it. This program makes that magic box for you!
// This program makes a function that adds a specific number
function magicbox(input_num) {
    let output = adder + input_num;
    return output;
}
function adders(num) {
    return num;
}
// Making a magic box that adds 2
let adder = adders(2);
console.log(magicbox(5));
